apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yml -o k8s-manifests/
    kompose.version: 1.36.0 (ae2a39403)
  labels:
    io.kompose.service: favourite-service
  name: favourite-service
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: favourite-service
  template:
    metadata:
      labels:
        io.kompose.service: favourite-service
    spec:
      serviceAccountName: favourite-service
      automountServiceAccountToken: true
      imagePullSecrets:
        - name: acr-secret
      containers:
        - env:
            # H2 en memoria para prod
            - name: SPRING_DATASOURCE_URL
              value: jdbc:h2:mem:favourite_service_db;DB_CLOSE_DELAY=-1;MODE=MYSQL
            - name: SPRING_DATASOURCE_DRIVER_CLASS_NAME
              value: org.h2.Driver
            - name: SPRING_DATASOURCE_USERNAME
              value: sa
            - name: SPRING_DATASOURCE_PASSWORD
              value: ""
            - name: SPRING_JPA_DATABASE_PLATFORM
              value: org.hibernate.dialect.H2Dialect
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: validate
            - name: SPRING_FLYWAY_ENABLED
              value: "true"
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              value: http://service-discovery:8761/eureka/
            - name: EUREKA_INSTANCE_HOSTNAME
              value: favourite-service
          envFrom:
            - configMapRef:
                name: common-environment-variables
          image: microservicesacr5fa48984.azurecr.io/favourite-service:prod
          imagePullPolicy: Always
          name: favourite-service
          ports:
            - containerPort: 8800
              protocol: TCP
          resources:
            requests:
              memory: "384Mi"
              cpu: "300m"
              ephemeral-storage: "512Mi"
            limits:
              memory: "512Mi"
              cpu: "500m"
              ephemeral-storage: "1Gi"
          readinessProbe:
            httpGet:
              path: /favourite-service/actuator/health
              port: 8800
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /favourite-service/actuator/health
              port: 8800
            initialDelaySeconds: 150
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: favourite-service
  name: favourite-service
  namespace: prod
spec:
  ports:
    - name: "8800"
      port: 8800
      targetPort: 8800
  selector:
    io.kompose.service: favourite-service
